[user]
     name = Máximo De León
     email = maximodleon@gmail.com
     username = maximodleon

[alias]
     l = log --date=short --decorate --pretty=format:"%C(yellow)%h\\ %ad%Cred%d\\ %Creset%s%Cblue\\ [%cn]"
     dog = log --all --decorate --oneline --graph
     co = checkout
     st = status
     p = pull
     pp = push
     c = commit
     cm = commit -m
     can = commit --amend --no-edit

[core]
     editor = nvim
     excludeFile = ~/.gitignore_global
    
[blame]
     date = relative
[commit]
     verbose = true
[status]
     showUntrackedFile = all
